services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: contextvault
      POSTGRES_PASSWORD: contextvault
      POSTGRES_DB: contextvault
    ports:
      - "5432:5432"
    volumes:
      - contextvault_pg:/var/lib/postgresql/data
volumes:
  contextvault_pg:
